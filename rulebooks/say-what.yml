---
- name: say thanks
  hosts: localhost
  gather_facts: false
  tasks:
    - debug:
        msg: "host:  {{ host }} --- FileSystem: {{ fs }}"
    - debug:
        msg: "Thank you, {{ event.sender | default('my friend') }}!"
